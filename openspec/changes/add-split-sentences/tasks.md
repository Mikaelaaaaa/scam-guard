## 1. 常數與遮罩

- [x] 1.1 於 `scam_guard/normalize.py` 定義 `SENTENCE_END = "。!?;"`，
      以註解說明為何不含 ASCII 句點、逗號與頓號
- [x] 1.2 定義 `URL_PATTERN`：`https?://\S+` 與 `www\.\S+`
- [x] 1.3 `_protected_spans(text) -> list[tuple[int, int]]` 回傳不可切區間

## 2. 切句實作

- [x] 2.1 `split_sentences(norm: NormalizedText) -> list[tuple[str, str]]`，
      回傳 (正規化片段, 原文片段) 序對
- [x] 2.2 掃描正規化後文字，遇到分隔符且不在保護區間內時斷句
- [x] 2.3 句末標點納入句子；換行作為斷點但不納入句子
- [x] 2.4 未以分隔符結尾的尾段成句
- [x] 2.5 對每個片段計算去除前後空白後的正規化區間 `[a, b)`
- [x] 2.6 去除後為空的片段整段略過，不進入結果
- [x] 2.7 以 `norm.raw_span(a, b)` 取得原文片段
- [x] 2.8 docstring 寫明：規則層需子句精度時應在句內自行以逗號再切，
      但 `evidence` 仍回報句子編號；連結至 `add-split-sentences` 的 design

## 3. 測試

- [x] 3.1 `tests/test_split_sentences.py`：句號、驚嘆號、問號、分號、換行各自切分
- [x] 3.2 逗號與頓號不切分，且切分後仍保留在句子內
- [x] 3.3 ASCII 句點不切分（網域、小數各一例）
- [x] 3.4 句末標點保留；換行不出現在句子內容中
- [x] 3.5 連續分隔符、空行、尾端分隔符皆不產生空句
- [x] 3.6 全為空白的輸入回傳空陣列
- [x] 3.7 無標點結尾的輸入成一句
- [x] 3.8 句子前後空白被去除，句內空白數量不變
- [x] 3.9 正規化片段與原文片段等長
- [x] 3.10 含零寬字元的輸入：正規化片段不含、原文片段含
- [x] 3.11 半形句號寫法：正規化片段收斂、原文片段保留原寫法
- [x] 3.12 帶查詢字串的網址（含 `?` 與 `;`）不被切開，網址後的句號正常切分
- [x] 3.13 重複呼叫結果一致
- [x] 3.14 迴歸案例：「您的驗證碼是 123456，請勿告訴他人」為單一句子，
      且句中逗號存在（此案例是規則層子句切分的前提）

## 4. 驗證

- [x] 4.1 `pytest tests/test_split_sentences.py` 全數通過
- [x] 4.2 `ruff check .` 通過
- [x] 4.3 確認 `split_sentences` 未新增任何專案內模組的 import
