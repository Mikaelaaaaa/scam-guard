"""請求路徑上的對外查詢 —— 本專案唯一會在一次偵測之中連上網路的地方。

**`net/` 與 `tools/` 是兩個目錄，不得合併。**

    tools/   部署前由人執行一次，產物是一份本機檔案（PSL 快照、165 黑名單快照、
             RDAP bootstrap 快照）。執行它的是建置腳本或維運人員，不是使用者的請求。
    net/     在每一次 `detect()` 中執行，延遲直接加在使用者的等待時間上，
             每一次執行都是一次對外流量。

分開的理由是可稽核性，不是整齊。`tools/` 目前成立的那句話 ——「它是離線的，
線上服務不碰它」—— 只要 `api/` 寫下一行 `import tools.something`，就在沒有任何
錯誤訊息的情況下失效了。兩者放同一個目錄時，「這個 import 會不會讓請求上網」
必須靠讀程式碼回答；分開之後靠目錄名就能回答。

因此：**`api/`、`adapters/`、`app.py` MUST NOT import `tools/`**。
需要快照時讀 `tools/` 產出的**檔案**，不執行 `tools/` 的程式。

`scam_guard/` MUST NOT import 本套件 —— 偵測核心只認識 Protocol，
由組裝層決定要不要把 `net/` 的實作注入進去。不注入時系統照常運作，
少一個訊號，而且在預設設定下不產生任何對外連線。
"""
